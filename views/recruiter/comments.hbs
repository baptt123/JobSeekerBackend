<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Quản lý Bình luận | TechConnect</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet" />
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
  <script>tailwind.config = { theme: { extend: { colors: { primary: '#6b26d9' } } } }</script>
  <style>body { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-slate-50 text-slate-900 h-screen flex overflow-hidden">

{{> recruiter_sidebar activePage='comments'}}

<main class="flex-1 flex flex-col min-w-0 overflow-hidden">
  <header class="h-16 bg-white border-b border-slate-200 flex items-center justify-between px-8">
    <h1 class="text-xl font-bold text-slate-800">Bình luận công việc</h1>
    <span class="text-sm text-slate-500">Quản lý tương tác trên các bài đăng của bạn</span>
  </header>

  <div class="flex-1 overflow-y-auto p-8">
    <div class="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden">
      <table class="w-full text-left border-collapse">
        <thead class="bg-slate-50 border-b border-slate-200">
        <tr>
          <th class="p-4 text-sm font-semibold text-slate-600">Ứng viên</th>
          <th class="p-4 text-sm font-semibold text-slate-600">Bình luận</th>
          <th class="p-4 text-sm font-semibold text-slate-600">Bài đăng</th>
          <th class="p-4 text-sm font-semibold text-slate-600">Ngày</th>
          <th class="p-4 text-sm font-semibold text-slate-600 text-right">Hành động</th>
        </tr>
        </thead>
        <tbody class="divide-y divide-slate-100">
        {{#each comments}}
          <tr class="hover:bg-slate-50 transition group">
            <td class="p-4">
              <div class="flex items-center gap-3">
                <img src="{{this.user.avatar_url}}" onerror="this.src='https://ui-avatars.com/api/?name={{this.user.full_name}}'" class="w-8 h-8 rounded-full object-cover">
                <div>
                  <p class="font-medium text-slate-800 text-sm">{{this.user.full_name}}</p>
                  <p class="text-xs text-slate-500">{{this.user.email}}</p>
                </div>
              </div>
            </td>

            <td class="p-4">
              <p class="text-sm text-slate-700 bg-slate-50 p-2 rounded-lg border border-slate-100 inline-block max-w-md">
                {{this.content}}
              </p>
            </td>

            <td class="p-4">
              <a href="/recruiter/jobs/{{this.job.job_id}}" class="text-sm text-indigo-600 hover:underline font-medium">
                {{this.job.title}}
              </a>
            </td>

            <td class="p-4 text-sm text-slate-500">
              {{this.createdAt}}
            </td>

            <td class="p-4 text-right">
              <button onclick="deleteComment({{this.id}})" class="text-slate-400 hover:text-red-600 p-2 rounded-full hover:bg-red-50 transition" title="Xóa bình luận">
                <span class="material-symbols-outlined text-xl">delete</span>
              </button>
            </td>
          </tr>
        {{/each}}

        {{#unless comments}}
          <tr>
            <td colspan="5" class="p-10 text-center text-slate-400">
              <span class="material-symbols-outlined text-4xl mb-2">chat_bubble_outline</span>
              <p>Chưa có bình luận nào trên các bài đăng của bạn.</p>
            </td>
          </tr>
        {{/unless}}
        </tbody>
      </table>
    </div>
  </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
<script>
  async function deleteComment(id) {
    if(!confirm('Bạn có chắc muốn xóa bình luận này?')) return;
    try {
      await axios.delete(`/recruiter/comments/${id}`);
      window.location.reload();
    } catch (e) {
      Toastify({
        text: "❌ Lỗi khi xóa bình luận",
        duration: 3000,
        gravity: "top",
        position: "right",
        style: { background: "#ef4444" }
      }).showToast();
      console.error(e);
    }
  }
</script>
</body>
</html>